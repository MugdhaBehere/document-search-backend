package com.example.document_search_backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = DocumentSearchAPI.class) // Ensure the main class is specified
public class DocumentSearchBackendApplicationTests {
    @Test
    void contextLoads() {
        // Empty test to check if the context loads
    }
}
